<UserControl x:Class="UrbanDiagnosticCentre.Views.PackageManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:UrbanDiagnosticCentre.Helpers">
    <UserControl.Resources><BooleanToVisibilityConverter x:Key="BoolToVisibility"/><helpers:StringToVisibilityConverter x:Key="StringToVisibility"/></UserControl.Resources>
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Border Grid.Row="0" Background="{StaticResource SidebarBrush}" Height="64" Padding="24,0">
            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <Button Grid.Column="0" Command="{Binding NavigateToDashboardCommand}" Style="{StaticResource GhostButton}" Content="← Dashboard" Height="40" Padding="12,0" Margin="0,0,20,0"/>
                <StackPanel Grid.Column="1" VerticalAlignment="Center"><TextBlock Text="Bundle Builder" FontSize="17" FontWeight="Bold" Foreground="White"/><TextBlock Text="Group your own services into reusable bundles" FontSize="11" Foreground="#7EB3F5"/></StackPanel>
                <Button Grid.Column="2" Command="{Binding AddNewCommand}" Style="{StaticResource PrimaryButton}" Content="+ Create Bundle" Height="40" Padding="18,0"/>
            </Grid>
        </Border>
        <Grid Grid.Row="1" Margin="20"><Grid.ColumnDefinitions><ColumnDefinition Width="340"/><ColumnDefinition Width="16"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="{StaticResource CardBrush}" CornerRadius="12" BorderBrush="{StaticResource DividerBrush}" BorderThickness="1"><DockPanel>
                <Border DockPanel.Dock="Top" Padding="14"><TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputField}" Height="40" ToolTip="Search bundles"/></Border>
                <ItemsControl ItemsSource="{Binding Packages}"><ItemsControl.ItemTemplate><DataTemplate><Border BorderBrush="{StaticResource DividerBrush}" BorderThickness="0,0,0,1" Padding="14,12"><Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions><TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/><TextBlock Grid.Row="1" Text="{Binding Description}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" TextTrimming="CharacterEllipsis" Margin="0,4,0,6"/><StackPanel Grid.Row="2" Orientation="Horizontal"><TextBlock Text="₹" Foreground="{StaticResource PrimaryBrush}"/><TextBlock Text="{Binding PriceDisplay}" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" Margin="2,0,10,0"/><TextBlock Text="{Binding TestCount}" Foreground="{StaticResource TextSecondaryBrush}"/><TextBlock Text=" services" Foreground="{StaticResource TextSecondaryBrush}" Margin="2,0,8,0"/><Button Content="Edit" Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Style="{StaticResource RowActionButton}"/></StackPanel></Grid></Border></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
            </DockPanel></Border>
            <Border Grid.Column="2" Background="{StaticResource CardBrush}" CornerRadius="12" BorderBrush="{StaticResource DividerBrush}" BorderThickness="1" Visibility="{Binding IsFormVisible, Converter={StaticResource BoolToVisibility}}">
                <ScrollViewer VerticalScrollBarVisibility="Auto"><StackPanel Margin="22">
                    <TextBlock Text="{Binding FormTitle}" Style="{StaticResource HeadingMedium}" Margin="0,0,0,20"/>
                    <TextBlock Text="Bundle Name *" Style="{StaticResource LabelText}"/><TextBox Text="{Binding FormName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputField}" Margin="0,0,0,14"/>
                    <TextBlock Text="Description" Style="{StaticResource LabelText}"/><TextBox Text="{Binding FormDescription, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputField}" Height="60" TextWrapping="Wrap" Margin="0,0,0,14"/>
                    <TextBlock Text="Bundle Price" Style="{StaticResource LabelText}"/><TextBox Text="{Binding FormPrice, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputField}" Margin="0,0,0,14"/>
                    <TextBlock Text="Add Your Services" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,10"/>
                    <TextBox Text="{Binding TestSearch, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputField}" ToolTip="Search services to add" Margin="0,0,0,8"/>
                    <ListBox ItemsSource="{Binding AvailableTests}" SelectedItem="{Binding SelectedAvailable}" Height="130" Margin="0,0,0,8"><ListBox.ItemTemplate><DataTemplate><Button Content="{Binding TestName}" Command="{Binding DataContext.AddTestToPackageCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}" HorizontalContentAlignment="Left" Background="Transparent" BorderThickness="0" Padding="8"/></DataTemplate></ListBox.ItemTemplate></ListBox>
                    <TextBlock Text="Selected Services" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,8,0,8"/>
                    <ItemsControl ItemsSource="{Binding PackageContents}"><ItemsControl.ItemTemplate><DataTemplate><Grid Margin="0,0,0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="{Binding TestName}" Foreground="{StaticResource TextPrimaryBrush}"/><Button Grid.Column="1" Content="Remove" Command="{Binding DataContext.RemoveTestFromPackageCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Style="{StaticResource RowActionButton}"/></Grid></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
                    <Border Background="#FEF2F2" CornerRadius="8" Padding="10" Margin="0,10,0,14" Visibility="{Binding FormError, Converter={StaticResource StringToVisibility}}"><TextBlock Text="{Binding FormError}" Foreground="{StaticResource ErrorBrush}" TextWrapping="Wrap"/></Border>
                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="10"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><Button Grid.Column="0" Command="{Binding CancelCommand}" Style="{StaticResource OutlineButton}" Content="Cancel"/><Button Grid.Column="2" Command="{Binding SaveCommand}" Style="{StaticResource PrimaryButton}" Content="Save Bundle"/></Grid>
                </StackPanel></ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>