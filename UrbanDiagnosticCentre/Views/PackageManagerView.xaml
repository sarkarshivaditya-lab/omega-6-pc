<UserControl x:Class="UrbanDiagnosticCentre.Views.PackageManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:UrbanDiagnosticCentre.Helpers">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <helpers:StringToVisibilityConverter x:Key="StringToVisibility"/>
        <helpers:InverseBoolConverter x:Key="InverseBool"/>

        <Style x:Key="PkgHeaderBtn" TargetType="Button">
            <Setter Property="Height"          Value="40"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="FontFamily"      Value="Segoe UI"/>
            <Setter Property="Background"      Value="#18FFFFFF"/>
            <Setter Property="Foreground"      Value="White"/>
            <Setter Property="BorderBrush"     Value="#3A5A84"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Padding"         Value="18,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#2AFFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionBtn" TargetType="Button">
            <Setter Property="Height"          Value="30"/>
            <Setter Property="FontSize"        Value="12"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="FontFamily"      Value="Segoe UI"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Padding"         Value="10,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AddTestBtn" TargetType="Button">
            <Setter Property="Width"          Value="30"/>
            <Setter Property="Height"         Value="30"/>
            <Setter Property="FontSize"       Value="16"/>
            <Setter Property="FontWeight"     Value="Bold"/>
            <Setter Property="Foreground"     Value="White"/>
            <Setter Property="Background"     Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"         Value="Hand"/>
            <Setter Property="Margin"         Value="0,0,4,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{StaticResource PrimaryDarkBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RemoveTestBtn" TargetType="Button">
            <Setter Property="Width"          Value="28"/>
            <Setter Property="Height"         Value="28"/>
            <Setter Property="FontSize"       Value="13"/>
            <Setter Property="Foreground"     Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="Background"     Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"         Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#FEE2E2"/>
                                <Setter Property="Foreground" Value="{StaticResource ErrorBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PanelHeader" TargetType="Border">
            <Setter Property="Background"      Value="#F0F4F9"/>
            <Setter Property="BorderBrush"     Value="{StaticResource DividerBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Padding"         Value="12,9"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ===== Header ===== -->
        <Border Grid.Row="0" Background="{StaticResource SidebarBrush}" Height="64" Padding="24,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Command="{Binding NavigateToDashboardCommand}"
                        Style="{StaticResource PkgHeaderBtn}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE830;" FontSize="14" FontFamily="Segoe MDL2 Assets"
                                   VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="Dashboard" FontSize="13" FontFamily="Segoe UI"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="20,0,0,0">
                    <TextBlock Text="Diagnostic Packages"
                               FontSize="17" FontWeight="Bold" FontFamily="Segoe UI"
                               Foreground="White"/>
                    <TextBlock Text="Manage reusable test bundles"
                               FontSize="11" FontFamily="Segoe UI" Foreground="#7EB3F5"/>
                </StackPanel>

                <Button Grid.Column="2"
                        Command="{Binding AddNewCommand}"
                        Style="{StaticResource PrimaryButton}"
                        Content="+ New Package"
                        Height="40" Padding="18,0" FontSize="13"/>
            </Grid>
        </Border>

        <!-- ===== Content ===== -->
        <Grid Grid.Row="1" Margin="24,20,24,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="340"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- ── Left: Package list ── -->
            <Border Grid.Column="0"
                    Background="{StaticResource CardBrush}"
                    CornerRadius="12"
                    BorderBrush="{StaticResource DividerBrush}"
                    BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="18" ShadowDepth="2" Color="#000000" Opacity="0.05"/>
                </Border.Effect>
                <DockPanel>
                    <!-- Search -->
                    <Border DockPanel.Dock="Top" Padding="12,10">
                        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource InputField}"
                                 Height="40"
                                 ToolTip="Search packages by name or description"/>
                    </Border>

                    <!-- Empty state -->
                    <StackPanel DockPanel.Dock="Bottom"
                                HorizontalAlignment="Center" VerticalAlignment="Center"
                                Margin="0,20">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Packages.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <TextBlock Text="No packages yet"
                                   FontSize="14" FontFamily="Segoe UI"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Click '+ New Package' to create one"
                                   FontSize="12" FontFamily="Segoe UI"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   HorizontalAlignment="Center" Margin="0,4,0,0"/>
                    </StackPanel>

                    <!-- Package rows -->
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding Packages}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{StaticResource DividerBrush}"
                                            BorderThickness="0,0,0,1"
                                            Padding="14,10">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="4"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="8"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <!-- Name + active badge -->
                                            <Grid Grid.Row="0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0"
                                                           Text="{Binding Name}"
                                                           FontSize="14" FontWeight="SemiBold"
                                                           FontFamily="Segoe UI"
                                                           Foreground="{StaticResource TextPrimaryBrush}"
                                                           TextTrimming="CharacterEllipsis"/>
                                                <Border Grid.Column="1" CornerRadius="4" Padding="6,2"
                                                        Margin="6,0,0,0">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Background" Value="#DCFCE7"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                                    <Setter Property="Background" Value="#F1F5F9"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <TextBlock Text="{Binding StatusDisplay}"
                                                               FontSize="10" FontWeight="SemiBold">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                                        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </Border>
                                            </Grid>

                                            <!-- Description -->
                                            <TextBlock Grid.Row="2"
                                                       Text="{Binding Description}"
                                                       FontSize="11" FontFamily="Segoe UI"
                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                       TextTrimming="CharacterEllipsis"
                                                       Visibility="{Binding Description, Converter={StaticResource StringToVisibility}}"/>

                                            <!-- Price + test count + actions -->
                                            <Grid Grid.Row="4">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <StackPanel Grid.Column="0" Orientation="Horizontal">
                                                    <TextBlock Text="₹" FontSize="12"
                                                               Foreground="{StaticResource PrimaryBrush}"
                                                               FontWeight="SemiBold" VerticalAlignment="Center"/>
                                                    <TextBlock Text="{Binding PriceDisplay}"
                                                               FontSize="13" FontWeight="Bold"
                                                               FontFamily="Segoe UI"
                                                               Foreground="{StaticResource PrimaryBrush}"
                                                               VerticalAlignment="Center" Margin="2,0,8,0"/>
                                                    <TextBlock FontSize="11" FontFamily="Segoe UI"
                                                               Foreground="{StaticResource TextSecondaryBrush}"
                                                               VerticalAlignment="Center">
                                                        <Run Text="{Binding TestCount, Mode=OneWay}"/>
                                                        <Run Text=" tests"/>
                                                    </TextBlock>
                                                </StackPanel>

                                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                                    <Button Content="Edit"
                                                            Style="{StaticResource ActionBtn}"
                                                            Background="#EBF3FF"
                                                            BorderBrush="{StaticResource DividerBrush}"
                                                            Foreground="{StaticResource PrimaryBrush}"
                                                            Margin="0,0,4,0"
                                                            Command="{Binding DataContext.EditCommand,
                                                                RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                            CommandParameter="{Binding}"/>
                                                    <Button Content="Copy"
                                                            Style="{StaticResource ActionBtn}"
                                                            Background="#F0FDF4"
                                                            BorderBrush="{StaticResource DividerBrush}"
                                                            Foreground="{StaticResource SuccessBrush}"
                                                            Margin="0,0,4,0"
                                                            Command="{Binding DataContext.DuplicateCommand,
                                                                RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                            CommandParameter="{Binding}"/>
                                                    <Button Content="Delete"
                                                            Style="{StaticResource ActionBtn}"
                                                            Background="#FEF2F2"
                                                            BorderBrush="#FECACA"
                                                            Foreground="{StaticResource ErrorBrush}"
                                                            Command="{Binding DataContext.DeleteCommand,
                                                                RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                            CommandParameter="{Binding}"/>
                                                </StackPanel>
                                            </Grid>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </DockPanel>
            </Border>

            <!-- ── Right: Package form (visible when IsFormVisible) ── -->
            <Border Grid.Column="2"
                    Visibility="{Binding IsFormVisible, Converter={StaticResource BoolToVisibility}}"
                    Background="{StaticResource CardBrush}"
                    CornerRadius="12"
                    BorderBrush="{StaticResource DividerBrush}"
                    BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="18" ShadowDepth="2" Color="#000000" Opacity="0.05"/>
                </Border.Effect>

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="24,20,24,24">

                        <!-- Form title -->
                        <TextBlock Text="{Binding FormTitle}"
                                   Style="{StaticResource HeadingMedium}"
                                   Margin="0,0,0,20"/>

                        <!-- Package fields -->
                        <Grid Margin="0,0,0,14">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Package Name *" Style="{StaticResource LabelText}"/>
                                <TextBox Text="{Binding FormName, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource InputField}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Price (₹) *" Style="{StaticResource LabelText}"/>
                                <TextBox Text="{Binding FormPrice, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource InputField}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="4" VerticalAlignment="Bottom">
                                <CheckBox Content="Active"
                                          IsChecked="{Binding FormIsActive}"
                                          FontSize="14" FontFamily="Segoe UI"
                                          Foreground="{StaticResource TextPrimaryBrush}"
                                          VerticalAlignment="Center"
                                          Height="44" Cursor="Hand"/>
                            </StackPanel>
                        </Grid>

                        <StackPanel Margin="0,0,0,20">
                            <TextBlock Text="Description" Style="{StaticResource LabelText}"/>
                            <TextBox Text="{Binding FormDescription, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource InputField}"
                                     ToolTip="Optional description for this package"/>
                        </StackPanel>

                        <!-- Test selection heading -->
                        <TextBlock Text="INCLUDED TESTS"
                                   FontSize="10" FontWeight="Bold" FontFamily="Segoe UI"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="0,0,0,10"/>

                        <!-- Test picker: two columns -->
                        <Grid Height="280">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Available tests -->
                            <Border Grid.Column="0"
                                    BorderBrush="{StaticResource DividerBrush}" BorderThickness="1"
                                    CornerRadius="10">
                                <DockPanel>
                                    <Border DockPanel.Dock="Top" Style="{StaticResource PanelHeader}"
                                            CornerRadius="10,10,0,0">
                                        <TextBlock Text="AVAILABLE TESTS"
                                                   FontSize="10" FontWeight="Bold" FontFamily="Segoe UI"
                                                   Foreground="{StaticResource TextSecondaryBrush}"/>
                                    </Border>
                                    <Border DockPanel.Dock="Top" Padding="8,6">
                                        <TextBox Text="{Binding TestSearch, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource InputField}"
                                                 Height="36"
                                                 ToolTip="Filter tests"/>
                                    </Border>
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <ItemsControl ItemsSource="{Binding AvailableTests}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Grid Height="40" Background="Transparent">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <StackPanel Grid.Column="0" VerticalAlignment="Center"
                                                                    Margin="10,0,4,0">
                                                            <TextBlock Text="{Binding TestName}"
                                                                       FontSize="12" FontFamily="Segoe UI"
                                                                       Foreground="{StaticResource TextPrimaryBrush}"
                                                                       TextTrimming="CharacterEllipsis"/>
                                                            <TextBlock Text="{Binding Category}"
                                                                       FontSize="10" FontFamily="Segoe UI"
                                                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                                                        </StackPanel>
                                                        <Button Grid.Column="1"
                                                                Content="+"
                                                                Style="{StaticResource AddTestBtn}"
                                                                VerticalAlignment="Center"
                                                                Command="{Binding DataContext.AddTestToPackageCommand,
                                                                    RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                                CommandParameter="{Binding}"/>
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </DockPanel>
                            </Border>

                            <!-- Package contents -->
                            <Border Grid.Column="2"
                                    BorderBrush="{StaticResource DividerBrush}" BorderThickness="1"
                                    CornerRadius="10">
                                <DockPanel>
                                    <Border DockPanel.Dock="Top" Style="{StaticResource PanelHeader}"
                                            CornerRadius="10,10,0,0">
                                        <TextBlock Text="IN THIS PACKAGE"
                                                   FontSize="10" FontWeight="Bold" FontFamily="Segoe UI"
                                                   Foreground="{StaticResource TextSecondaryBrush}"/>
                                    </Border>

                                    <!-- Empty hint -->
                                    <Border DockPanel.Dock="Bottom"
                                            Visibility="{Binding HasNoPackageTests, Converter={StaticResource BoolToVisibility}}"
                                            VerticalAlignment="Stretch">
                                        <TextBlock Text="&#x2190; Add tests from the left"
                                                   FontSize="12" FontFamily="Segoe UI"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"/>
                                    </Border>

                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <ItemsControl ItemsSource="{Binding PackageContents}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Grid Height="40" Background="Transparent">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <StackPanel Grid.Column="0" VerticalAlignment="Center"
                                                                    Margin="10,0,4,0">
                                                            <TextBlock Text="{Binding TestName}"
                                                                       FontSize="12" FontFamily="Segoe UI"
                                                                       Foreground="{StaticResource TextPrimaryBrush}"
                                                                       TextTrimming="CharacterEllipsis"/>
                                                            <TextBlock Text="{Binding Category}"
                                                                       FontSize="10" FontFamily="Segoe UI"
                                                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                                                        </StackPanel>
                                                        <Button Grid.Column="1"
                                                                Content="&#xE711;"
                                                                FontFamily="Segoe MDL2 Assets"
                                                                Style="{StaticResource RemoveTestBtn}"
                                                                VerticalAlignment="Center"
                                                                Command="{Binding DataContext.RemoveTestFromPackageCommand,
                                                                    RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                                CommandParameter="{Binding}"/>
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </DockPanel>
                            </Border>
                        </Grid>

                        <!-- Validation error -->
                        <Border Background="#FEF2F2" CornerRadius="8" Padding="14,10" Margin="0,16,0,0"
                                Visibility="{Binding FormError, Converter={StaticResource StringToVisibility}}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE7BA;  " FontSize="14" FontFamily="Segoe MDL2 Assets"
                                           Foreground="{StaticResource ErrorBrush}" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding FormError}"
                                           FontSize="13" FontFamily="Segoe UI"
                                           Foreground="{StaticResource ErrorBrush}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>

                        <!-- Action buttons -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                            <Button Content="Cancel"
                                    Command="{Binding CancelCommand}"
                                    Style="{StaticResource SecondaryButton}"
                                    Width="100" Height="40" Margin="0,0,10,0"/>
                            <Button Content="Save Package"
                                    Command="{Binding SaveCommand}"
                                    Style="{StaticResource PrimaryButton}"
                                    Width="140" Height="40"/>
                        </StackPanel>

                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Right placeholder when form is hidden -->
            <Border Grid.Column="2">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsFormVisible}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Border Width="60" Height="60" CornerRadius="14"
                            Background="#EBF3FF" HorizontalAlignment="Center" Margin="0,0,0,16">
                        <TextBlock Text="&#xE71D;" FontSize="28" FontFamily="Segoe MDL2 Assets"
                                   Foreground="{StaticResource PrimaryBrush}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="Select a package to edit"
                               FontSize="15" FontFamily="Segoe UI"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="or click '+ New Package' to create one"
                               FontSize="12" FontFamily="Segoe UI"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>

        </Grid>
    </Grid>
</UserControl>
